# Legacy agents

The classes in this folder are legacy, rule-based agents kept for compatibility with early experiments and tests. They rely on modules like `embeddings`, `guardrails`, and `rag` that may not be present in all deployments.

New development should use the API-mediated patterns in `hf_space` and the shared utilities in `ai/common`.

